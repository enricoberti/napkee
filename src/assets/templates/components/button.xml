<?xml version="1.0" encoding="utf-8"?>
<component>
	<html>
	<![CDATA[
<button id="${id}" type="button" disabled="${disabled}" class="napkeeComponent napkeeButton ${style}">${code}</button>
	]]>
	</html>
	<css>
	<![CDATA[
#${id}{
	left:${left};
	top:${top};
	z-index:${z};
}

#${id}.napkee {
	width:${width};
	height:${height};
	color:${color};
	font-size: ${fontSize};
	font-style:${fontStyle};
	font-weight:${fontWeight};
	text-decoration:${textDecoration};
}

	]]>
	</css>
	<jsdocready>
	<![CDATA[
$("#${id}").click(function(event){
	${clickCode}
});
	]]>
	</jsdocready>
	<jsimport>
	</jsimport>
	<cssimport>
	</cssimport>
</component>