<?xml version="1.0" encoding="utf-8"?>
<component>
	<html>
	<![CDATA[
<fieldset id="${id}" class="napkeeComponent napkeeFieldSet">
	<legend>${label}</legend>
	${children}
</fieldset>
	]]>
	</html>
	<css>
	<![CDATA[
#${id}{
	left:${left};
	top:${top};
	width:${width};
	height:${height};
	z-index:${z};
	font-size: ${fontSize};
	font-style:${fontStyle};
	font-weight:${fontWeight};
	text-decoration:${textDecoration};
	opacity:${opacity};
	filter:alpha(opacity=${opacityIE});
	background-color:${color};
	margin:0;
	padding:0;
}
	]]>
	</css>
	<jsdocready>
	</jsdocready>
	<jsimport>
	</jsimport>
	<cssimport>
	</cssimport>
</component>