<?xml version="1.0" encoding="utf-8"?>
<component>
	<html>
	<![CDATA[
<button id="${id}" class="napkeeComponent napkeeMultilineButton ${style}"> 
   ${code} 
</button> 
	]]>
	</html>
	<css>
	<![CDATA[
#${id}{
	left:${left};
	top:${top};
	z-index:${z};
}

#${id}.napkee {
	width:${width};
	height:${height};
	background-color:${color};
	color:${labelColor};
	opacity:${opacity};
	filter:alpha(opacity=${opacityIE});
}
	]]>
	</css>
	<jsdocready>
	<![CDATA[
$("#${id}").click(function(event){
});
	]]>
	</jsdocready>
	<jsimport>
	</jsimport>
	<cssimport>
	</cssimport>
</component>