<?xml version="1.0" encoding="utf-8"?>
<component>
	<html>
	<![CDATA[
<div id="${id}" class="napkeeComponent napkeeParagraph" cellspacing="0" cellpadding="0">${code}</div>
	]]>
	</html>
	<css>
	<![CDATA[
#${id}{
	left:${left};
	top:${top};
	width:${width};
	height:${height};
	z-index:${z};
	font-size: ${fontSize};
	font-style:${fontStyle};
	font-weight:${fontWeight};
	text-decoration:${textDecoration};
	text-align:${textAlign};
	color:${color};
	white-space: normal;
}
	]]>
	</css>
	<jsdocready>
	<![CDATA[
$("#${id}").click(function(event){
	${clickCode}
});
	]]>
	</jsdocready>
	<jsimport>
	</jsimport>
	<cssimport>
	</cssimport>
</component>