<?xml version="1.0" encoding="utf-8"?>
<component>
	<html>
	<![CDATA[
<input id="${id}" type="text" placeholder="${label}" class="napkeeComponent napkeeSearchbox search-query" disabled="${disabled}" />
	]]>
	</html>
	<css>
	<![CDATA[
#${id}{
	left:${left};
	top:${top};
	color:${color};
	width:${newWidth};
	height:${newHeight};
	z-index:${z};
	font-size: ${fontSize};
	font-style:${fontStyle};
	font-weight:${fontWeight};
	text-decoration:${textDecoration};
}
	]]>
	</css>
	<jsdocready>
	<![CDATA[
$("#${id}").click(function(event){
	${clickCode}
});
	]]>
	</jsdocready>
	<jsimport>
	</jsimport>
	<cssimport>
	</cssimport>
</component>