<?xml version="1.0" encoding="utf-8"?>
<component>
	<html>
	<![CDATA[
<div id="${id}" class="napkeeComponent napkeeStickynote">${label}</div>
	]]>
	</html>
	<css>
	<![CDATA[
#${id}{
	left:${left};
	top:${top};
	width:${width};
	height:${height};
	z-index:${z};
	font-size: ${fontSize};
	font-style:${fontStyle};
	font-weight:${fontWeight};
	text-decoration:${textDecoration};
	background-color:${color};
}
	]]>
	</css>
	<jsdocready>
	<![CDATA[
$("#${id}").draggable();
	]]>
	</jsdocready>
	<jsimport>
	</jsimport>
	<cssimport>
	</cssimport>
</component>