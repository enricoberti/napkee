<?xml version="1.0" encoding="utf-8"?>
<component>
	<html>
	<![CDATA[
<textarea id="${id}" disabled="${disabled}" class="napkeeComponent napkeeTextarea">${label}</textarea>
	]]>
	</html>
	<css>
	<![CDATA[
#${id}{
	left:${left};
	top:${top};
	width:${width};
	height:${height};
	z-index:${z};
	color:${labelColor};
	background-color:${color};
	opacity:${opacity};
	filter:alpha(opacity=${opacityIE});
	font-size: ${fontSize};
	font-style:${fontStyle};
	font-weight:${fontWeight};
	text-decoration:${textDecoration};
	text-align:${textAlign};
}
	]]>
	</css>
	<jsdocready>
	</jsdocready>
	<jsimport>
	</jsimport>
	<cssimport>
	</cssimport>
	
</component>